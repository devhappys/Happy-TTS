registry=https://registry.npmjs.org/

# 网络配置
fetch-retries=3
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=60000
# 允许依赖安装脚本（CI 用 --ignore-scripts 覆盖）；需要构建的包由 pnpm-workspace.yaml 的 allowBuilds 白名单管理
ignore-scripts=false
