# frontend/docs

This directory is intentionally retired.

The former Docusaurus documentation site is no longer part of this project:

- root build scripts do not build it;
- Docker images do not copy or publish it;
- Express does not serve `/docs`;
- CI and Dependabot do not treat this directory as a package.

Use `/api-docs` and `/api/openapi.json` for API documentation.
