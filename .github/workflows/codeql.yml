name: "CodeQL"

# Disabled: project rate limiting is centralized in src/middleware/routeLimiters.ts.
# Re-enable only if a future security review explicitly requires CodeQL scanning again.
on:
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    if: false
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: CodeQL disabled
        run: echo "CodeQL workflow is disabled."
