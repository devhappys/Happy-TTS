# Happy-TTS legacy codebase audit report

- Updated: 2026-07-31
- Scope and exclusions: Repository-wide read-only audit of first-party backend, frontend, persistence, configuration, deployment, CI, tests, and operator-facing documentation. Generated outputs, dependency/vendor directories, `.git/`, `.trellis/`, `.agents/`, and the `tame-legacy-codebase/` audit toolkit are excluded from product LOC; they may still be inspected as workflow evidence where relevant.
- Environment and limitations: Static inspection on the shared Windows workspace. Repository instructions prohibit local build, test, type-check, lint, and installation commands; those checks are not run locally and CI workflow definitions are inspected instead. Existing user changes are preserved and excluded from any audit commit.
- Mode: 系统
- Decisions: E0 / G0 / Q0 / C1 / D0
- Report status: Waiting for decisions (audit complete; five Suspected findings remain open)

## Before assessment (frozen audit baseline)

Security        ██░░░░░░░░  2.5  D   Recoverable passwords/MFA/runtime secrets; split auth/IP/WS boundaries; frontend install TLS disabled
Stability       ███░░░░░░░  3.0  C   Destructive fail-green deploy, stale config/lost-update paths, webhook loss, and no coordinated drain
Performance     ████░░░░░░  4.5  C   Static-only evidence: broad DOM scans, repeated Mongo initialization, missing deadlines, and silent mmap fallback
Testing         ██░░░░░░░░  2.5  D   Frontend suite absent from CI; global boundary mocks, no-op/copied/weak suites, and no coverage baseline
Maintainability ███░░░░░░░  3.5  C   229,504 broad source LOC, many mixed-responsibility units, stale scripts/specs, and ownership gaps
Design          ███░░░░░░░  3.0  C   Session, secret, configuration, chat, and persistence authorities are duplicated or contradictory
Release         █░░░░░░░░░  1.5  D   Deployment is not gated promotion; host/artifact identity, attestations, health cutover, and rollback are weak
─────────────────────────────────────
Overall         ██░░░░░░░░  2.9  D

| Dimension | Confidence | Scope/evidence |
| --- | --- | --- |
| Security | High | Authentication/authorization variants, passwords/MFA/runtime/provider secrets, CORS/proxy/IP/WS boundaries, browser storage, dependency TLS, SSH and CI permissions |
| Stability | High | Startup/config caching, Mongo authority, multi-instance persistence, external deadlines, event acknowledgement, shutdown, deployment failure handling |
| Performance | Low | Static hot-path/resource evidence only; no representative production workload, profile, mutation-rate, provider-latency, or fallback telemetry |
| Testing | High | Test inventory/config/setup/assertions, collection paths, frontend/backend/browser/integration CI workflows, coverage configuration; execution prohibited |
| Maintainability | High | LOC/threshold inventory, responsibility mixing, import reachability, duplication, change hotspots, scripts/specs/docs and ownership |
| Design | High | Layer and trust boundaries, session/config/data authorities, cross-layer consumers, retry/idempotency/lifecycle contracts |
| Release | High | GitHub workflows, image construction/publication, dependency graph, container configuration, SSH deployment, readiness, rollback, provenance and operator docs |

## After assessment

Not applicable for E0/G0/Q0. No product code, contract, configuration, data, or runtime behavior will be changed.

## Finding summary

| Severity | Count | Confirmed | Suspected |
| --- | ---: | ---: | ---: |
| Critical | 0 | 0 | 0 |
| High | 30 | 30 | 0 |
| Medium | 43 | 39 | 4 |
| Low | 7 | 7 | 0 |
| Info | 1 | 0 | 1 |
| **Total** | 81 | 76 | 5 |

## Executive summary

- Current runnable state: Not locally executed; repository policy requires actual build/test commands to run in GitHub workflows.
- Main conclusion: The repository has substantial implemented functionality and several positive controls, but identity/session authority, secret handling, persistence ownership, test authenticity, and release promotion are split across incompatible paths. The most serious failures are deterministic rather than stylistic: browser cookie sessions are rejected by `/api/auth/me`, administrators can recover users' original passwords, MFA/runtime credentials are broadly persisted or projected, guest chat ownership can collide, and production deployment can bypass verification and report success after destructive failure.
- Highest-priority findings: Freeze unverified production promotion first (F-002/F-003/F-013/F-014), then repair the server-side auth/serialization boundary before widening cookie use (F-004/F-007/F-008/F-043-F-046), and remove recoverable password/MFA/runtime secrets from ordinary storage and response projections (F-029/F-030/F-035/F-046).
- Next smallest action: Make the production deployment job depend on required checks for the exact image digest and fail closed, while a separate security slice removes `strict-ssl=false` and prevents current-user/admin serializers from returning authentication material.

## Code size baseline

| Area/type | Files | Physical lines | Exclusions/notes |
| --- | ---: | ---: | --- |
| Broad first-party source, excluding tests | 802 | 229,504 | Audit-tool source classification; includes runtime code plus first-party scripts/helpers, physical lines including comments and blanks |
| Runtime product subset | 727 | 213,168 | Backend `src/`: 404 / 106,296; frontend `frontend/src/`: 269 / 100,130; Rust: 54 / 6,742 |
| Tests | 122 | 17,917 | Audit-tool test classification across backend, frontend, browser, integration, and Rust paths |
| Configuration/CI/deployment navigation bucket | 46 | 27,238 | Manifests, lockfiles, workflows, Docker/Compose, tool configuration, and deployment scripts; overlaps source and must not be added to it |
| Documentation | 40 | 14,481 | Tracked Markdown in audit scope; `.trellis/` and `.agents/` excluded from LOC but inspected as workflow evidence |
| Whole selected tracked tree | 1,039 text files | 296,660 | 262,429 nonblank physical lines plus four binary files; generated/dependency/build directories excluded |

| Largest file/symbol | Physical lines | Role | Finding ID or rationale |
| --- | ---: | --- | --- |
| `frontend/src/utils/integrityCheck.ts` | 2,758 | Browser integrity/recovery singleton | F-011/F-022; largest runtime source navigation point |
| `frontend/src/components/EnvManager.tsx` | 2,522 | Environment administration UI | F-018; current worktree file, size used only as navigation evidence |
| `scripts/fix-dependabot-alerts.js` | 2,108 | Dependency/security maintenance automation | Tooling hotspot, not itself a defect |
| `frontend/src/components/CDKStoreManager.tsx` | 1,989 | CDK store workflow UI | F-018 |
| `frontend/src/components/LibreChatPage.tsx` | 1,872 | Chat UI/state/network orchestration | F-018 and LibreChat contract findings |
| `frontend/src/components/LogShare.tsx` | 1,841 | Log sharing/import/export UI | F-016/F-017/F-018 |
| `src/services/ecoEnchantsService.ts` | 1,794 | Licensing/entitlement business service | F-018/F-059 |
| `scripts/deploy_image.js` | 1,780 | Production SSH/Docker deployment orchestrator | F-002/F-003/F-014 |
| `src/controllers/adminController.ts` | 1,733 | Cross-domain administrator controller | F-018/F-029/F-030 |
| `frontend/src/App.tsx` | 1,704 | Root routing/auth/layout/modal assembly | F-007/F-015/F-018 |
| `src/routes/index.ts` | 1,688 | Global route/security mount assembly | F-018/F-040/F-044 |

Runtime-product thresholds used only for navigation: 164 files are at least 400 lines, 66 at least 800, 36 at least 1,200, and 13 at least 1,600. Size is not treated as a finding without a demonstrated responsibility/coupling failure.

Change hotspots from Git history since 2026-01-01 (commit path-touch count, not defect count): root `package.json` 201, `pnpm-lock.yaml` 182, frontend lockfile 171, frontend manifest 170, `frontend/src/App.tsx` 50, `src/config/config.ts` 40, `Dockerfile` 38, `src/controllers/authController.ts` 34, `frontend/src/components/TicketSystem.tsx` 34, `src/controllers/adminController.ts` 32, `frontend/src/components/EnvManager.tsx` 31, `src/app.ts` 31, `.github/workflows/tsc.yml` 29, `src/app/assembly.ts` 28, and `src/routes/index.ts` 24. The overlap with auth/config/release findings increases remediation priority but is not evidence by itself.

## Baseline checks

| Check | Command/evidence | Result | Baseline failure? |
| --- | --- | --- | --- |
| Working tree | `git status --short` | Existing user changes present before audit | No; protected baseline |
| Build/test/type-check/lint | Repository instruction | Not run locally; prohibited outside GitHub workflows | Unverified, not a failure claim |
| Static inventory | `python -B tame-legacy-codebase/scripts/inventory_codebase.py . --format json --exclude .trellis --exclude .agents` | 802 broad first-party source files / 229,504 physical lines; 122 test files / 17,917 lines | No; read-only navigation baseline |
| Static evidence search | `rg`, `git ls-files`, UTF-8 `Get-Content`, and targeted Git history | Routes, imports, projections, workflows, repeated call sites, hotspots, and documentation contracts inspected | Findings recorded below; no runtime claim inferred from search alone |
| CI/runtime history | Repository and registry interfaces | Workflow definitions inspected; Actions history, branch protection, environments, secrets, registry attestations, production hosts, databases, logs, metrics, and traffic unavailable | Unverified external state, explicitly retained as residual risk |

## Before/after comparison

| Measure | Before (frozen) | After | Delta | Interpretation |
| --- | --- | --- | --- | --- |
| Runtime product physical LOC | 213,168 across 727 files | N/A | N/A | E0 audit; broad source classification is 229,504 across 802 files |
| Largest relevant file/symbol | `frontend/src/utils/integrityCheck.ts`, 2,758 lines | N/A | N/A | Navigation metric; F-011/F-022 provide the actual failure evidence |
| Tests / static checks | Local execution prohibited | N/A | N/A | CI definitions inspected instead |
| Confirmed findings fixed/open | All confirmed findings remain Open; final count is synchronized with the finding summary | N/A | N/A | No remediation authorized |
| Documented functional standards | README/operator docs, route/API contracts, schemas/storage adapters, tests, Docker/Compose/workflows, LICENSE/package metadata, and Trellis guidance inspected | N/A | N/A | Conflicts are findings rather than silently choosing one source |
| User-visible/contract behavior | Unchanged | Unchanged | 0 | E0/G0/Q0 |

## Functional standards and contract sources

- Runtime behavior and trust boundaries: `src/app.ts`, `src/app/assembly.ts`, `src/app/startup.ts`, route mount order, authentication/CORS/replay/rate-limit middleware, controllers, services, models, storage adapters, frontend API clients/hooks, and WebSocket code.
- Public/operator contracts: `readme.md`, `openapi.json` plus route annotations, `docs/BACKEND_INTEGRATION_CONTRACT.md`, persistence/deployment/test-matrix documents, Dockerfiles, Compose, package scripts, and GitHub workflows.
- Security/legal metadata: `LICENSE`, package manifests and lockfiles, runtime configuration validation, cookie/JWT/passkey/TOTP behavior, secret projections, workflow permissions, artifact publication, and deployment scripts.
- Verification evidence: backend/frontend/browser/integration tests and their global setup/configuration. A test name or workflow label is not accepted as proof when mocks, ignore rules, or copied mini-servers bypass the production boundary.
- Authority rule used in this report: executable current code and concrete consumer behavior outrank stale prose; explicit LICENSE text outranks contradictory package metadata; product intent that cannot be derived locally remains `Suspected` and is listed under pending decisions.

## System map

- User journeys: browser registration/login (password, TOTP, Passkey, OAuth), protected/admin navigation, TTS generation/history, chat, CDK/resource/admin operations, email, artifacts/log sharing, webhook ingestion, and WebSocket notifications.
- Browser boundary: a Vite/React SPA uses a shared Axios client plus multiple independent Axios/fetch paths, HttpOnly cookie sessions with some bearer exceptions, local/session storage, and a WebSocket query-token path. Auth state is instantiated in many hooks rather than owned by one provider.
- Application boundary: Express assembly applies core parsing/request IDs, security/CORS/Helmet/WAF/rate-limit/auth variants, routes/static delivery, and error handling. Startup initializes diagnostics, embedded Rust workers, permissions, Mongo/user storage, billing reconciliation, schedulers, HTTP, and WebSocket.
- Data authorities: Mongo `user_datas` is the main user authority; `runtime_config_settings` is the configuration authority with a mutable process cache; `USER_STORAGE_MODE` also permits MySQL/file modes; disk holds uploads/audio/share logs and fallback JSON. LibreChat currently has Mongo, process memory, and `data/chat_history.json` authorities without one concurrency protocol.
- Internal runtime units: the production image contains Node/Express, the built React SPA, and five Rust binaries (network, audio, file, data, security), optionally exposed as Compose sidecars or started in-process and reached through shared-memory/file-backed IPC or HTTP fallback.
- External trust boundaries: OpenAI-compatible providers, Mongo/Redis, Resend/Svix, GitHub, LinuxDo, DeepLX, IPQS/Scamalytics, IPFS/image-bed endpoints, public network/media providers, Docker Hub, GitHub Actions, SSH, and production Docker hosts.
- Delivery flow: pull requests run partially overlapping TypeScript/Jest/Rust/governance/browser/CodeQL jobs; the Docker workflow independently builds and publishes branch/short-SHA tags, then SSH-deploys, replaces containers, prunes images, and uploads a log. It is not promotion of the artifact proven by the required verification jobs.
- Principal failure boundaries: browser-to-session authority, middleware-to-controller auth context, administrator-to-secret storage, Express-to-Mongo/cache ownership, multi-instance persistence, webhook/provider retry semantics, process shutdown, image promotion, SSH host identity, and rollback/readiness.

## Finding ledger

| ID | Severity | Evidence | Disposition | Finding | Location/evidence | Impact | Action or question |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | High | Confirmed | Open | The global Axios response interceptor retries every HTTP method after network errors, timeouts, or 5xx responses without an idempotency requirement. | `frontend/src/api/api.ts:135-213`; 71 statically identified `api.post/put/patch/delete` call sites are listed below. Concrete non-idempotent/high-impact consumers include command execution (`CommandManager.tsx:214,258`), outbound email (`EmailSender.tsx:403-429`), CDK redemption/generation/deletion (`api/cdks.ts:73-160`), and user deletion/bulk action (`UserManagement.tsx:462,492`). | If the server commits a request but its response is lost, the client replays the operation. This can duplicate emails or commands and repeat account, credential, billing/penalty, ticket, resource, or administrative mutations. Recovery depends on undocumented endpoint-specific idempotency and is not enforced by the client. | Restrict transparent retries to safe methods (`GET`/`HEAD`) and explicitly opted-in idempotent operations. For retryable mutations require a stable idempotency key and server-side deduplication; add a regression/contract test for “commit succeeded, response lost.” |
| F-002 | High | Confirmed | Open | The production deployment job can destroy the running container, fail to start its replacement, prune the rollback image, and still exit successfully. | Deployment is automatic after publish in `.github/workflows/docker.yml:151-199`. `execSSHCommand` resolves non-zero remote exits as data (`scripts/deploy_image.js:235-258`), while deploy-path callers do not test `result.code` at lines 274, 290, 337, 624, 638, 644, 657, 993, 1003, 1014, 1020, and 1038. The old container is renamed and removed before replacement (`637-657`); replacement gets only fixed sleeps and no health/readiness gate (`988-1028`); `docker image prune -a -f` follows (`1035-1043`, invoked at `1245-1246`). Errors are caught/logged without propagation (`266-279`, `611-657`, `1026-1028`, `1247-1249`) and the script prints success at `1269`; invalid aggregate configuration also returns with exit 0 (`1183-1194`). | Image pull failure, Docker option incompatibility, port collision, network attachment failure, or an immediately crashing container can produce a green Actions job while service is down. Pruning may remove the last local rollback image, and the overwritten inspect JSON backup is not an automated restore path. | Make every required remote non-zero exit throw; aggregate per-server/container outcomes and set a non-zero process exit; retain the old container/image until the new container passes readiness/health checks; automatically restore on failure; add production environment approval and deployment concurrency controls. |
| F-003 | High | Confirmed | Open | Deployment SSH accepts every host key in both CI and non-CI paths. | `scripts/deploy_image.js:185-225`: CI sets `hostVerifier = () => true` at line 205; the purported strict branch calls `callback(true)` unconditionally at lines 207-211. The workflow supplies production SSH credentials at `.github/workflows/docker.yml:190-199`. | A DNS, routing, or host impersonation attack can receive the deployment connection and commands. The pipeline cannot authenticate that it deployed the image to the intended server, exposing production credentials and deployment control. | Store the expected host public-key fingerprint/`known_hosts` material as protected configuration, compare it fail-closed (constant-time where appropriate), rotate it deliberately, and remove all accept-all branches. |
| F-004 | High | Confirmed | Open | Active frontend modules unconditionally send `Authorization: Bearer null` during the documented cookie-only browser session flow, overriding a valid HttpOnly cookie on the backend. | Browser login explicitly clears JS tokens and saves an empty token (`frontend/src/hooks/useAuth.ts:316-338`); `getAuthToken()` then returns `null` (`frontend/src/utils/authSession.ts:27-45`). The backend prefers any non-empty Bearer value before checking the cookie (`src/utils/authCookie.ts:74-87`), so the literal string `null` is treated as the credential. Nineteen active occurrences are listed below; routes/imports in `frontend/src/App.tsx` and `frontend/src/components/admin/adminModules.tsx` confirm normal reachability. | Authenticated users can receive deterministic 401/403 failures in broadcast administration, backup-code/TOTP management, CDK management, FBI data administration, and LogShare despite possessing a valid session cookie. Some native `fetch`/fresh Axios clients also bypass the shared cookie-aware client, widening configuration drift. | Omit `Authorization` unless a real injected token exists; route browser traffic through the shared `withCredentials` client; add cookie-only integration coverage for each affected feature and a server test proving malformed/placeholder Bearer values do not mask a valid cookie. |
| F-005 | Medium | Confirmed | Open | The nightly “live / integration” workflow selects only test files that its chosen Jest configuration explicitly ignores. | `.github/workflows/nightly-test-matrix.yml:29-53` starts Mongo and runs `pnpm run test:nightly`; `package.json:58` selects `logshare-mongodb\|policyApi\|ipfs-upload\|network-apis\|media-social-life-apis\|ip-query\|yiyan-api`, while `jest.ci.config.js:4-20` adds an ignore pattern for exactly that same set. Additionally, `src/tests/policyApi.test.js:255-301` is a standalone `runTests()` script with no Jest `describe`/`it`/`test`, and catches its own failures. | The scheduled workflow cannot provide the claimed integration health signal: it resolves to no eligible tests (or an empty-suite failure if the ignore mismatch is changed without converting `policyApi`). Persistent red/no-op nightly jobs create alert fatigue and conceal real external-service regressions. | Add a dedicated live Jest configuration that does not inherit the live-test ignore list; convert `policyApi.test.js` to real Jest assertions or run it as an explicit Node smoke command with non-zero failure propagation; assert a known test count in CI. |
| F-006 | Medium | Confirmed | Open | The test named as a MongoDB application integration test globally replaces Mongoose and the application Mongo service with in-memory mocks. | The default config loads `src/tests/setup.ts` for every suite (`jest.config.js:6-10`). That setup mocks `mongoose` (`setup.ts:268-276`) and `../services/mongoService` as connected (`397-407`). `src/tests/logshare-mongodb.test.ts:12-41` calls those mocked APIs and queries the mock model while claiming the upload “should be stored in MongoDB.” The separate `tests/integration/mongo-replica.test.js` uses real Mongo but tests raw driver transaction/TTL behavior, not the LogShare application path. | Schema, driver, connection, serialization, collection-name, index, and persistence regressions can pass the nominal LogShare Mongo suite. Maintainers receive a false boundary guarantee and may ship storage failures despite green unit tests. | Run live application persistence tests under an isolated Jest config/process without the global mocks, point them at disposable Mongo, assert actual collection writes/reads and cleanup, and keep the mock-based suite clearly named as a unit/route test. |
| F-007 | High | Confirmed | Open | Authentication state is locally instantiated at 36 static hook call sites, and the active `LoginPage` state does not update the root route guard. | `useAuth` owns local `useState` values (`frontend/src/hooks/useAuth.ts:113-127`) and only performs its auth check in its own mount effect (`185-268`); it is not backed by a Context/external store. Root `App` has one instance (`frontend/src/App.tsx:549-550`) that drives `ProtectedRoute`/`AdminRoute` (`503-516`, `649-671`), while `LoginPage` has another (`frontend/src/components/LoginPage.tsx:105-106`). When `redirectTo` exists, successful login performs SPA `navigate` without reload (`LoginPage.tsx:136-150`); the root instance remains `user === null` and immediately redirects the protected target back to login. The 36-site list is static reachability evidence, not a claim that every candidate is mounted simultaneously. | The intended “return to the originally requested protected/admin page after login” path can loop or fail even though the server cookie was created. The normal no-redirect path hides the split via `window.location.reload()`, and active components can observe mutually inconsistent auth/loading/account state. | Introduce one `AuthProvider`/external store with a single revalidation path. As an immediate containment, successful redirect logins must trigger the root store to revalidate (or hard reload); add a browser test that starts on a protected URL, logs in, and reaches the original URL. |
| F-008 | High | Confirmed | Open | WebSocket authentication uses a reusable query-string JWT, diverges from cookie sessions, and trusts stale authorization claims. | Passkey completion returns `token` in JSON without the session-cookie helper (`src/routes/passkeyRoutes.ts:443-452,561-570`); `loginWithToken` persists it in session storage (`frontend/src/hooks/useAuth.ts:355-361`, `frontend/src/utils/authSession.ts:35-40`); and `useWebSocket` appends it as `?token=` (`frontend/src/hooks/useWebSocket.ts:29-53`). The server reads only that query JWT, verifies its signature, and derives `isAdmin` from embedded claims without reloading the current user, suspension, deletion, or role (`src/services/wsService.ts:145-203`). Cookie-only users therefore connect anonymously (`frontend/src/hooks/useWebSocket.ts:86-92`), while an old administrator or suspended user can retain privileged subscriptions until token expiry. | Query credentials can enter browser/proxy diagnostics and be replayed. Authorization changes are not reflected in established/query-authenticated connections, privileged broadcasts can reach stale identities, and normal cookie users miss targeted notifications. | Authenticate upgrades from the canonical cookie/session context or a single-use, audience-bound, short-lived WebSocket ticket; load current user/role and reject invalid sessions instead of silently treating them as anonymous; disconnect sessions on suspension/role change and redact query credentials from all logs. |
| F-009 | Medium | Confirmed | Open | The advertised saved-account switcher cannot switch ordinary cookie-only accounts and gives the user no actionable transition. | The product copy promises fast same-device account switching (`frontend/src/components/WelcomePage.tsx:49-53`) and saved account cards call `switchAccount` (`120-133`, `286-301`). Storage deliberately strips every account token (`frontend/src/utils/authSession.ts:62-71`); browser login also saves an empty token (`frontend/src/hooks/useAuth.ts:332-335`). For such accounts, `switchAccount` only clears the current state and navigates to `/welcome`, the page already being shown (`useAuth.ts:270-285`). | Clicking a normal saved account does not select it, open a login form, prefill identity, or provide feedback. The visible core feature is therefore a dead-end interaction, while restoring long-lived bearer storage would reintroduce the XSS risk the cookie migration was meant to remove. | Reframe the card action as “sign in again as this account,” navigate to `/login` with a safe account identifier/prefill, and preserve the return path; or implement a server-side multi-session switch contract. Update product copy and add a cookie-only account-card browser scenario. |
| F-010 | Medium | Confirmed | Open | A documented and navigable ModList capability is deliberately disabled while its full editor remains orphaned. | The application still exposes `/modlist` (`frontend/src/App.tsx:715`) and the admin navigation advertises “模组列表” (`frontend/src/navigation/navConfig.ts:131-136`); README history lists ModList as a delivered capability (`readme.md:1062-1065`). `frontend/src/components/ModListPage.tsx:5-25` only shows “已临时关闭” because of runtime exceptions and does not load `ModListEditor`; the 953-line `frontend/src/components/ModListEditor.tsx` has no static consumer. | Users following a formal product entry cannot complete the task. The repository carries a large inactive implementation without a tracked owner, failing scenario, restoration criterion, or retirement decision. | Reproduce and fix the runtime failure behind a feature-level test, then restore the editor; or explicitly retire the capability by removing navigation/current-feature claims while preserving migration/compatibility notes. Record an owner and deadline either way. |
| F-011 | Medium | Confirmed | Open | The client integrity checker’s emergency recovery destroys the live React application and does not remount it. | Proxy-tamper detection immediately calls `performEmergencyRecovery` (`frontend/src/utils/integrityCheck.ts:840-859`). Recovery assigns the captured markup to `document.documentElement.innerHTML` (`878-899`), which replaces the DOM and event listeners; `reinitializeCriticalElements` only rebuilds integrity hashes (`906-912`). No `ReactDOM.createRoot`, hydration, or hard reload exists in this recovery path. | Any true or false-positive trigger can turn the page into an apparently restored but non-interactive static snapshot, breaking navigation, forms, dialogs, state, accessibility focus, and cleanup semantics. The 2,758-line singleton then continues timers against replaced nodes. | Never restore an application by replacing the document HTML. Fail safely with a minimal isolated warning and a hard reload/static recovery page, or constrain recovery to explicitly owned text nodes; add a browser test that triggers the path and proves the application remains interactive. |
| F-012 | High | Confirmed | Open | The documented “recommended” Docker Compose production path omits secrets that startup validation requires, so the container cannot start as documented. | `docker-compose.yml:12-50` sets `NODE_ENV=production` but does not inject `JWT_SECRET`, `SIGN_SECRET_KEY`, or `ADMIN_PASSWORD`. Current startup validation rejects production without all three (`src/config/config.ts:246-267`). README twice recommends `docker-compose up -d` (`readme.md:498-518,917-946`), and its embedded Compose example also omits the required variables. A Compose `.env` file supplies interpolation values only for keys referenced in `environment`; it does not implicitly inject unrelated variables into the container. | A new operator following the primary deployment instructions receives a crash-loop/fail-fast startup instead of a service. This blocks delivery, encourages ad-hoc weakening of production validation, and leaves no documented post-start health verification. | Declare required variables with fail-fast interpolation such as `${JWT_SECRET:?required}`, `${SIGN_SECRET_KEY:?required}`, and `${ADMIN_PASSWORD:?required}`; provide a non-secret `.env.example` checklist and a documented health/log verification step. Keep the application’s production fail-fast behavior. |
| F-013 | High | Confirmed | Open | Production image publication and deployment start directly from a branch push without waiting for the repository’s build, test, Rust, quality, or security workflows for the same commit. | `.github/workflows/docker.yml:3-12,61-75,151-199` publishes then deploys on pushes to `main`, `master`, and `dev`; its only dependency is deploy → publish inside the same workflow. Node/Jest/Rust verification lives independently in `.github/workflows/tsc.yml:3-14,20-52,77-132`, quality checks in `quality-guardrails.yml:3-14`, and CodeQL in `codeql.yml:3-13`; there is no production environment gate or `workflow_run` dependency. `dev` is especially exposed because verification workflows listen to `develop`, not `dev`. The Dockerfile/workflow builds artifacts but does not run the Jest/Rust test suites. | A pushed commit can reach production while its verification jobs are still running or are not running for that branch. A later test/security failure cannot stop or roll back the already-applied deployment, so an unverified SHA becomes live by design. | Promote one immutable image digest only after required checks for that exact SHA succeed. Gate deployment through a protected GitHub Environment/approval or a carefully validated `workflow_run`, align branch names, and verify the deployed digest matches the tested artifact. |
| F-014 | High | Confirmed | Open | The production deploy workflow permits concurrent runs, while its lock file exists only inside each ephemeral GitHub runner. | `.github/workflows/docker.yml` has no workflow/job `concurrency` or protected environment. `scripts/deploy_image.js:14-16,115-149` creates `scripts/.deploy_image.lock` in the checkout; separate hosted runners have separate filesystems and therefore acquire independent locks. Each run can concurrently rename/remove/recreate the same remote containers (`deploy_image.js:637-657,1013-1028`). | Closely spaced pushes can interleave destructive remote operations: one run may rename or remove a container while another inspects, deletes, connects networks, or prunes images. This can corrupt the deployment sequence, produce misleading success, or take the service down even when each image is individually valid. | Add workflow/environment-level concurrency keyed to the production target with `cancel-in-progress: false`, and acquire an atomic lock on the remote host as defense in depth. Keep deployment operations idempotent and verify the expected current image/container before mutation. |
| F-015 | Medium | Confirmed | Open | At least fourteen custom modal/dialog implementations lack the shared accessibility contract already demonstrated elsewhere in the application. | The following modal containers have no `role="dialog"`, `aria-modal`, labelled title relationship, or complete focus trap/restore behavior: `frontend/src/components/AlertModal.tsx:44`; `AnnouncementModal.tsx:42`; `BackupCodesModal.tsx:181,310`; `BroadcastModal.tsx:112`; `ConfirmModal.tsx:52`; `DebugInfoModal.tsx:52`; `FingerprintRequestModal.tsx:134`; `LibreChatRealtimeDialog.tsx:27`; `PasskeyVerifyModal.tsx:62`; `PromptModal.tsx:158` (partial Escape/key handling only); `ui/CredentialIdModal.tsx:18`; `user-management/RevealPasswordModal.tsx:46`; `DataCollectionManager.tsx:777,965,1013`; and the imperative Mermaid overlay at `Mermaid.tsx:118-180`. `frontend/src/App.tsx:1565-1611` is a repository-local positive example with dialog semantics and focus references. | Screen-reader users are not told a dialog opened; keyboard focus can move into background controls; critical confirmation, credential, 2FA, and backup-code workflows can become ambiguous or unsafe. Behavior and fixes are duplicated across many components. | Consolidate on an accessible dialog primitive (existing library or shared component) that supplies title/description linkage, `aria-modal`, initial focus, focus containment, Escape, background inertness/scroll lock, and focus restoration; add keyboard/accessibility tests for the critical dialogs. |
| F-016 | Medium | Confirmed | Open | Export options labelled “AES-256 encryption” use public bundle constants as keys and unauthenticated CBC, so they do not provide the implied confidentiality or tamper protection. | LogShare uses fixed `happy_logshare` (`frontend/src/utils/logShareStorage.ts:4-10`) with SHA-256-derived AES-CBC and no MAC (`144-169`) for export/import (`183-198,220-230`), while the UI labels it “AES-256 加密” (`frontend/src/components/LogShare.tsx:1363-1374`). Image export repeats the same design with fixed `happy_images` (`ImageUploadPage.tsx:62-64,213-237,930-976,1407-1415`). The currently unreferenced generic storage layer contains a third copy/default (`frontend/src/utils/localStorage.ts:35-42,75-100,423-454`). | Anyone receiving an export can recover the key from the public JavaScript bundle and decrypt it; unauthenticated CBC permits undetected ciphertext manipulation before import. Users may make privacy decisions based on a security label the implementation cannot satisfy. | Use a user-provided secret with a memory-hard/appropriate KDF, random salt/nonce, authenticated encryption (AES-GCM or equivalent), and a versioned format; otherwise relabel the option as obfuscation and remove confidentiality claims. Validate authentication before parsing imported data. |
| F-017 | Medium | Confirmed | Open | Frontend HTTP behavior is split across six Axios instances, thirteen direct Axios calls, and 228 native `fetch` call sites, with almost no visible application-level deadline/cancellation contract. | Axios instances: `frontend/src/api/api.ts:65`, `hooks/useAuth.ts:87`, `hooks/useTts.ts:16`, `components/BackupCodesModal.tsx:48`, `TOTPManager.tsx:56`, and `TOTPVerification.tsx:40`; the central instance itself has no `timeout` (`api/api.ts:60-71`). Direct Axios calls occur at `hooks/useTwoFactorStatus.ts:14` and `components/LogShare.tsx:276,295,314,328,350,383,410,530,539,579,634,673`. Static inventory found 228 `fetch(` calls outside frontend tests but only four explicit `signal:` uses (`FBIWantedPublic.tsx:74`, `DataCollectionManager.tsx:255,279`, `MobileNav.tsx:166`). These paths bypass different subsets of URL canonicalization, cookie/bearer policy, IP-verification/fingerprint headers, 401 handling, retry policy, and error normalization; F-004 is a realized example. | Weak or half-open networks can leave screens loading indefinitely or allow late responses to overwrite newer state. Authentication, retry, penalty, telemetry, and error semantics drift per page, making cross-cutting fixes scatter across hundreds of call sites. | Establish one typed request boundary with a default deadline and caller-propagated `AbortSignal`, single cookie/injected-bearer policy, normalized errors, and explicit safe retry/idempotency metadata. Migrate by high-risk flows first and add contract tests for cancellation, auth rejection, timeout, and retry behavior. |
| F-018 | Medium | Confirmed | Open | Several core frontend and backend units combine unrelated state, I/O, authorization, configuration, and domain workflows in one change surface. | Frontend shape counts (physical lines / `useState` / effects / request calls / handler-like functions) identify current-worktree `EnvManager.tsx` 2522/83/2/47/133; `CDKStoreManager.tsx` 1989/31/7/1/85; `LibreChatPage.tsx` 1872/17/8/14/37; `LogShare.tsx` 1841/18/7/12/62; `UserProfile.tsx` 1823/27/9/3/45; `App.tsx` 1704/12/21/4/27; `RuntimeConfigSections.tsx` 1474/33/1/19/61; and `WebhookEventsManager.tsx` 1398/17/1/15/90. Backend examples are `ecoEnchantsService.ts` 1,794; `adminController.ts` 1,733 across users/announcements/email/ModList/webhooks/config; `routes/index.ts` 1,688 across governance and mount orchestration; `dataCollectionService.ts` 1,685; and `runtimeConfigService.ts`, which owns many secret types plus the global cache. F-004/F-007/F-015/F-017 and F-030/F-032/F-044/F-059 demonstrate actual cross-cutting drift rather than size alone. | Ordinary auth, request, accessibility, persistence, or field-contract changes cross unrelated regions and duplicated handlers, making review, ownership, and isolated tests difficult. | Extract shared auth/request/config/persistence contracts first, then split listed units by use case and state owner behind characterization/contract tests. Use size only to navigate; require each extraction to reduce a demonstrated change reason or coupling. |
| F-019 | Low | Confirmed | Open | Twenty additional frontend source files (1,879 physical lines) have no static inbound import after checking normal imports, dynamic-import strings, and `import.meta.glob`; the separate 953-line `ModListEditor.tsx` candidate is tracked under F-010 and excluded here to avoid double counting. | Complete additional candidate list: `frontend/src/utils/localStorage.ts` (489); `hooks/useAntiCounterfeit.ts` (357); `components/AudioPreview.tsx` (219); `HCaptchaVerificationExample.tsx` (139); `AdminLogin.tsx` (95); `api/hcaptcha.ts` (79); `hooks/useHCaptchaConfig.ts` (71); `utils/test-tamper-api.ts` (71); `components/MarkdownExportPage/useKatex.ts` (62); `components/ui/scroll-area.tsx` (52); `types/security.ts` (47); `components/MarkdownExportPage/MarkdownPreview.tsx` (43); `hooks/useReducedMotion.ts` (39); `hooks/useTwoFactorStatus.ts` (31); `ThemeToggle.tsx` (29); `utils/fetchWithFallback.ts` (21); `layout/index.ts` (15); `navigation/index.ts` (9); `utils/escapeHtml.ts` (9); `components/ui/index.ts` (2). The HTML entry `main.tsx` is excluded from this rule. | These files increase search/review cost and retain conflicting patterns, including the third fixed-key AES implementation and a dead `Bearer null` hook. They can mislead maintainers even if the bundler tree-shakes them. | Confirm there is no build-external/plugin consumer or generated entry, then delete or move genuine examples into an explicit dev/examples area. Add an unused-export/source-entry check so the backlog does not regrow. |
| F-020 | High | Confirmed | Open | The approximately 100k-line frontend has only six test files, and its configured Vitest suite is never executed by repository CI. | Frontend production inventory: 269 source files / 100,130 physical lines; tests: six files / 789 lines (`integrityDiagnostics`, `useFirstVisitDetection`, `VerificationMethodSelector`, `TOTPSetup`, `scoringAlgorithm`, `LinuxDoAuthCallbackPage`). `frontend/package.json:15-26` defines `test: vitest` and `frontend/vitest.config.ts:5-13` configures the suite. Root Jest deliberately ignores frontend tests (`jest.config.js:20-22`), and no workflow contains `vitest` or a frontend test command; verification/guardrail jobs build the frontend only (`.github/workflows/tsc.yml:40-52`, `quality-guardrails.yml:60-89`). There is no coverage for the confirmed auth redirect/store split, cookie/Bearer matrix, automatic mutation retry, dialog focus, or major management components. | Critical browser authentication and side-effect behavior can regress or remain broken while all required repository checks pass. Large-component refactoring lacks characterization safety, and the existing six tests provide no CI signal at all. | Add a required PR job running `pnpm --dir frontend test --run` (with reports/artifacts), then prioritize tests for F-001/F-004/F-007, cookie-only/Passkey matrices, retry idempotency, dialog keyboard/focus behavior, and high-risk component contracts. |
| F-021 | Medium | Confirmed | Open | Public artifact CSV preview splits on commas/newlines instead of parsing CSV quoting rules. | `frontend/src/components/ArtifactSharePage.tsx:190-195` performs `split(/\r?\n/)` then `row.split(',')`; the public artifact renderer uses this result for every `csv` artifact (`386-387`). No contract limits CSV to unquoted fields. | Valid values such as `"Doe, John",42`, escaped quotes, or embedded newlines are silently rendered into the wrong rows/cells. Shared artifact viewers can display materially incorrect data without an error. | Use a maintained CSV parser or a small standards-compliant state machine with size limits; if the product intentionally supports only a simplified dialect, validate/reject quoted or embedded-delimiter content and document that constraint. Add golden tests for commas, quotes, CRLF, and multiline fields. |
| F-022 | Medium | Suspected | Open | Two always-on, overlapping whole-document anti-tamper scanners may impose sustained main-thread cost and amplify false-positive recovery risk. | `frontend/src/main.tsx:395-417,483-607` serializes/scans large DOM/window surfaces; it runs at DOMContentLoaded + 0.5/1.5/3s, observes all document child/subtree/attribute mutations, and polls every 20s (`610-666`). Independently, the imported singleton `frontend/src/utils/integrityCheck.ts:144-160,999-1051` observes `document.documentElement` for child/subtree/attributes/character data and polls recovery state every 500ms. Together the two implementations are 3,449 lines and have no production feature flag. | On large dynamic pages, mutation bursts can repeatedly invoke broad scans, consume the UI thread, retain long-lived timers/observers, and increase the probability of reaching F-011. Actual severity depends on production page sizes, mutation rates, devices, and false-positive telemetry, none of which is available in the repository. | Profile representative production pages/devices and record callback frequency, scan duration, long tasks, and false positives. If material, consolidate to scoped targets/events, disconnect on teardown, add a production kill switch, and remove full-DOM serialization from mutation callbacks. Question: is this always-on anti-tamper behavior a required product contract, and are production profiles/telemetry available? |
| F-023 | Medium | Confirmed | Open | The CI browser “cookie contract” test exercises a hand-written miniature server rather than production authentication code. | Playwright starts `node tests/browser/cookie-server.js` (`playwright.config.ts:20-24`), whose 61 lines duplicate the cookie name/options, parser, set/read/clear endpoints (`tests/browser/cookie-server.js:1-61`) without importing `src/utils/authCookie.ts`, the production routes, JWT verification, middleware order, or application assembly. `tests/browser/auth-cookie.spec.ts:3-30` asserts only that replica; the job is presented as `Browser cookie smoke` in `.github/workflows/quality-guardrails.yml:137-161`. | Production cookie naming, Secure/SameSite selection, expiry, clearing, Bearer-vs-cookie precedence, login/logout routing, or middleware changes can break while this required-looking browser job stays green. It validates browser cookie mechanics, not the application contract. | Start a test-configured real application and exercise actual login, `/api/auth/me`, protected navigation, and logout. If a minimal server is retained for browser mechanics, import a shared production cookie helper and rename the job to avoid claiming application coverage. |
| F-024 | Medium | Confirmed | Open | The repository has no mandatory lint/static-style gate, and its only Biome workflow is a manual auto-fix workflow that suppresses failures and excludes the frontend. | `.github/workflows/biome-check.yml:3-5` is `workflow_dispatch` only. Its safe and unsafe jobs run write-mode checks against `./src` with `\|\| true` (`34-35,104-105`), so diagnostics cannot fail the job. `biome.json:34-36` includes only `src/**/*.ts(x)`, and no other workflow invokes Biome/ESLint/lint. | Suspicious patterns, unused code/imports, formatting drift, and frontend issues do not block merge or the independently triggered production deployment. The manual fix workflow conflates enforcement with mutation and cannot serve as a trustworthy quality signal. | Add a read-only required `biome check` (or chosen linter) job covering backend and frontend, with failures propagated. Keep safe/unsafe auto-fix PR workflows separate and explicitly manual. |
| F-025 | High | Confirmed | Open | The global Jest setup replaces security, rate-limit, IP, user, and persistence boundaries for nearly every backend test, so route/controller “integration” suites do not assemble the production pipeline. | `jest.config.js:6-10` loads `src/tests/setup.ts` globally. Complete boundary replacements in that setup: IP check `4-6`; Mongoose `268-276`; tamper protection `278-281`; custom/express/route limiters `283-395`; Mongo service `397-407`; user service `409-497`; IP allow decision `499-509`; and local-IP config override `511-520`. Only `src/tests/rateLimit.test.ts:8-9` statically un-mocks two rate-limit modules. F-006 is a concrete false-integration consequence. | WAF/IP/rate-limit/tamper/auth-storage/middleware-order and real persistence regressions can remain invisible while broad supertest suites pass. The suite count therefore overstates protection of the most security-sensitive application assembly. | Keep global setup limited to deterministic environment/bootstrap helpers. Move mocks into explicitly named unit suites; create unmocked application/security-pipeline contract suites and disposable-database tests that exercise production middleware ordering and negative cases. |
| F-026 | Medium | Suspected | Open | The global Jest setup silences console output and installs process-level unhandled-error listeners that only log, which may mask asynchronous test failures depending on Jest listener ordering. | `src/tests/setup.ts:633-641` replaces every console method with `jest.fn()`. It registers `unhandledRejection` and `uncaughtException` handlers that only call the mocked console and neither rethrow nor mark a test failed (`710-717`). | Background Promise rejections or process-level exceptions may become false-green or, at minimum, lose their only diagnostics. Whether Jest’s own listeners still force failure for every timing scenario requires a deliberate CI fault-injection run, which local policy forbids. | Remove these process handlers or rethrow/fail explicitly; suppress console only per test and preserve output on failure. Question: do current GitHub runs include a fault-injection test proving an unhandled rejection/exception fails the job under this setup? |
| F-027 | Medium | Confirmed | Open | At least nineteen collected Jest tests are empty, tautological, or too weak to distinguish success from failure. | Complete confirmed set: four empty email tests with all requests/assertions commented out (`src/tests/emailController.test.ts:12-89`); one `true === true` placeholder (`behaviorDataGenerator.test.ts:2-5`); ten network/data tests asserting only `res.body` exists (`network-apis.test.ts:5-52`); replay protection’s missing-header assertion defaults absent evidence to the expected `400` (`replayProtection.test.ts:59-68`); and three cleanup tests that only exercise Promise/timer/Jest mechanics (`cleanup.test.ts:3-27`). | Test counts and green status overstate protected behavior. Email policy, network status/schema/negative cases, and replay rejection can regress while these tests continue passing or provide no signal. | Replace each placeholder with calls to production exports/routes and discriminating assertions on status, schema, side effects, and negative behavior; delete pure test-framework checks unless they protect a repository-specific harness invariant. |
| F-028 | Medium | Confirmed | Open | Multiple tests reimplement the algorithm or rule inside the test instead of importing production code. | Complete confirmed set: credential-ID logic is locally recreated or reduced to Node Buffer/regex behavior in `credential-id-fix.test.ts:2-39`, `credential-id-format-fix.test.ts:2-13`, `credential-id-format.test.ts:2-33`, `credential-id-padding.test.ts:1-10`, `actual-credential-id.test.ts:1-7`, and `simple-credential-id.test.ts:1-12`; `src/tests/useAuth.test.ts:5-143` defines its own auth/rate/error rules; `frontend/src/tests/scoringAlgorithm.test.ts:32-117` copies the scoring calculation embedded in `frontend/src/components/SmartHumanCheck.tsx:830-917`. | Production code can change or break while the copied test remains green. Duplication also freezes stale expectations and hides that the production rule is not exposed behind a testable boundary. | Extract production pure functions/contracts and import them directly, or test the real component/service behavior. Remove diagnostic scripts from the Jest suite or label them as non-gating examples. |

| F-029 | High | Confirmed | Open | User passwords are deliberately stored in reversible form and can be revealed to administrators. | `src/utils/passwordSecurity.ts:8-17,48-76,86-115` stores both a bcrypt hash and AES-256-GCM ciphertext; the password master key falls back to `config.jwtSecret` when `PASSWORD_ENCRYPTION_KEY`/`AES_KEY` is absent. `src/services/userService.ts:392-426` decrypts the original password, and `src/routes/admin/users.ts:78-106,293-365`, `src/controllers/adminController.ts:575-610`, and `frontend/src/components/UserManagement.tsx:559-584` implement a step-up-protected reveal UI. | Database plus JWT/master-key compromise, or an administrator-session compromise, can recover every revealable user's original password. Password reuse extends the blast radius to unrelated sites; step-up, rate limits, and audit logs do not remove the stored plaintext-equivalent secret. | Retain only a strong password hash and replace reveal with administrator-forced reset. If password escrow is an explicit product/compliance requirement, document it and use independent KMS/HSM custody, non-exportable keys, dual control, and a narrowly audited recovery workflow. |
| F-030 | High | Confirmed | Open | Runtime and operational secrets are persisted as ordinary plaintext Mongo fields. | The generic Mixed runtime model is `src/models/runtimeConfigModel.ts:24-42`. Plain setters/loaders cover IPQS (`src/services/runtimeConfigService.ts:610-680`), LinuxDo (`696-747`), signing (`964-1006`, current dirty worktree extension of the same existing pattern), DeepLX (`1022-1074`), NexAI (`1118-1153`), email (`1271-1323`), and administrator/status/short-link passwords (`1361-1402`). Outemail/ModList/webhook settings use schemas and writes in `src/controllers/adminController.ts:93-127,1150-1175,1700-1714`; LibreChat provider keys use `src/services/librechat/models.ts:56-67` and `src/routes/libreChatRoutes.ts:820-858`; short-link AES material is stored through `src/routes/shortUrlRoutes.ts:94-151`. No field-level envelope encryption or secret reference layer was found. | Read access to Mongo or a backup exposes provider keys, JWT/signing material, operational passwords, and integration credentials. Rotation and least-privilege are difficult because secrets share a generic configuration authority and broad read paths. | Store secret references or KMS-envelope ciphertext, separate secret and non-secret schemas/projections, restrict decryption to the consuming operation, add rotation/version metadata, and fail closed if a required secret cannot be decrypted. |
| F-031 | High | Confirmed | Open | Runtime security configuration initialization failure is logged but startup continues. | `src/services/mongoService.ts:137-152` catches `RuntimeConfigService.initialize(true)` failure, warns, and still returns a successful Mongo connection; `src/app/startup.ts:67-83` then treats storage initialization as complete. | Schema/query/normalization failures can leave JWT, signing, administrator, provider, or policy settings at defaults or stale cache values without a configuration generation proving what is active. The service can accept traffic in an unknowable security state. | Make required security configuration part of startup/readiness and fail closed. If degraded startup is intentional, expose the exact configuration generation, failed keys, and a readiness status that prevents traffic to affected capabilities. |
| F-032 | High | Confirmed | Open | The runtime-configuration cache can retain removed values and overwrite newer writes with an older reload snapshot. | `src/services/runtimeConfigService.ts:574-608` creates `nextCache` but mutates the old global cache during document iteration and repeatedly copies all fields; database-absent keys can survive. Setters write Mongo and then mutate the cache, for example `1144-1153,1393-1402`, with no lock, version, CAS, or single-flight coordination, so a force reload can publish a stale query snapshot after a newer setter. | Security and provider settings can roll back, persist after deletion, or differ between process memory and Mongo. Operators cannot reason about which credential/policy generation is active, and concurrent requests amplify the race. | Build one immutable snapshot off to the side and publish it atomically; serialize reload/write paths or use versioned CAS; clear absent keys explicitly; expose the active configuration generation and test concurrent set/reload ordering. |
| F-033 | Medium | Confirmed | Open | Request paths repeatedly call a non-single-flight `connectMongo()` that reconnects and force-reloads runtime configuration. | `src/services/mongoService.ts:69-152` has no connected/in-flight short circuit. Request-path callers include `src/controllers/humanCheckController.ts:179,278,308,328,360`, `src/routes/ipfsRoutes.ts:263`, `src/routes/logRoutes.ts:300,455,516,576,639,694,1036,1069`, `src/services/ipVerificationService.ts:181`, and `src/services/turnstile/trace.ts:14`. | Concurrent traffic can trigger redundant connection attempts, logging, and configuration queries, increasing latency and widening F-032's stale-cache race. | Own one process-level connection promise with state checks; request paths should await readiness rather than initialize infrastructure; separate explicit configuration refresh from connection establishment. |
| F-034 | Medium | Confirmed | Open | The production server-status endpoint has a predictable default password, `wmy`. | The default appears in `src/config/config.ts:119` and `src/services/runtimeConfigService.ts:38`; production validation at `src/config/config.ts:246-278` does not require an override. The public route is mounted at `src/routes/diagnosticsRoutes.ts:11` and checked by `src/controllers/diagnosticsController.ts:36-43`; current output includes uptime/CPU/heap in `src/services/operationalStatusService.ts:43-56`. | Internet-reachable deployments that keep the default expose operational telemetry behind a known shared secret and establish a precedent for default-password administration. | Require a high-entropy production value or use the canonical current-admin session/role; remove the default from executable configuration and add startup validation. |
| F-035 | High | Confirmed | Open | Reusable GitHub cookies and headers are stored in plaintext and duplicated across configuration authorities. | `src/services/githubBillingService.ts:26-50` defines stored credential fields; `516-552` saves `config1` and copies it into legacy settings; `978-999` sends the values as GitHub request credentials. | Database/backup disclosure yields reusable GitHub sessions. Duplicate storage creates multiple rotation and deletion surfaces and can leave one active copy behind. | Replace reusable browser cookies with least-privilege scoped tokens where possible; store only KMS-backed secret references/ciphertext in one authority; migrate, rotate, and delete legacy copies with reconciliation. |
| F-036 | Medium | Confirmed | Open | GitHub multi-configuration requests drop the separately stored Cookie credential. | `src/services/githubBillingService.ts:481-497` extracts Cookie from headers into `parsed.cookies`, and `516-540` persists both. The single-configuration path explicitly sends Cookie at `978-984`, but the multi-configuration path at `1394-1418` sends only `config.headers`. | Cookie-authenticated secondary configurations deterministically receive 401/authorization failures even though their saved configuration appears complete. | Use one request-construction function for every configuration and add a contract test covering Cookie- and token-authenticated multi-configuration requests. |
| F-037 | High | Confirmed | Open | LibreChat ownership/bearer tokens are written to persistent logs and also returned to browser JavaScript. | Full token strings are logged in `src/services/libreChatService.ts:460,489,547,601,724,777,836,871`; `/clear` logs a request body containing the token in `src/routes/libreChatRoutes.ts:404-413`; `/guest` sets an HttpOnly cookie but also returns the token in JSON at `128-147`. These tokens authorize history, updates, deletes, export, and SSE. | Anyone with log access, injected browser script, or copied diagnostics can replay the token to read or mutate chat data. HttpOnly protection is defeated by returning the same credential to JavaScript. | Rotate exposed tokens, make browser use cookie-only ownership, log only a non-secret digest/correlation ID, and enforce default sink redaction as part of F-059. |
| F-038 | High | Confirmed | Open | Tokenless LibreChat guests share one empty-string ownership namespace. | Guest mode permits `/send`, `/history`, `/retry`, and `/sse` without a token (`src/routes/libreChatRoutes.ts:257-290,337-365,564-581,666-691`) and passes `token ?? ""`; `src/services/libreChatService.ts:515-551,798-850,856-1050,1432-1458` uses `userId` or token as the ownership/filter key. Guest mode is enabled outside production by default and can be enabled in production (`src/routes/libreChatRoutes.ts:89-95`). | In any guest-enabled deployment, unrelated anonymous users can read, append, retry, or subscribe to the same history namespace. | Always issue a high-entropy server-side anonymous-session credential and require it for every chat/history/SSE mutation or read. If tokenless chat is intentionally a public shared room, make that explicit in UI/docs and isolate it from private history. |
| F-039 | High | Confirmed | Open | LibreChat token normalization maps distinct caller tokens to the same Mongo owner. | Tokens can come from body/query/header/cookie and are barely validated (`src/routes/libreChatRoutes.ts:76-86`). `src/services/libreChatService.ts:31-36` removes characters and truncates to 128 bytes; values such as `a!` and `a?`, or strings sharing the first normalized 128 characters, collide. Mongo reads/writes use the normalized key (`279-302,807-811,879-884,1436-1439`) while in-memory paths sometimes compare the original token (`829-835,862-867`). | Distinct users can collide on the same persisted history, and memory/Mongo ownership decisions can disagree. | Accept only server-issued token formats and store a cryptographic digest as the owner key; migrate existing keys and add collision/round-trip contract tests. |
| F-040 | High | Confirmed | Open | Most LibreChat routes do not establish the normal HTTP session context. | Only `/history` uses `optionalAuthenticateToken` (`src/routes/libreChatRoutes.ts:337`). `/message`, `/send`, `/clear`, `/messages`, `/retry`, `/export`, and `/sse` at `178,257,404,462,513,564,615,666` omit session middleware; mounts in `src/routes/index.ts:554-571,912-928` provide limits but not canonical auth. | A valid cookie session is treated as anonymous or rejected across core chat operations, while ad hoc ownership tokens become a second identity system with the collision/leak failures above. | Apply one canonical optional/required auth context to the complete route family and test cookie, bearer, guest, suspended, and anonymous matrices. |
| F-041 | High | Confirmed | Open | LibreChat persistence loses concurrent updates and can create duplicate owner documents. | `ChatHistorySchema.userId` has a non-unique index (`src/services/librechat/models.ts:45-51`); `src/services/libreChatService.ts:279-302` overwrites a whole message array with no version/CAS; each process has a separate global array; file fallback at `214-227` has no atomic rename or serialization. | Concurrent first writes can create duplicate owner documents, later `findOne` can select an arbitrary copy, and process snapshots can overwrite one another, losing messages. | Add a unique conversation/owner key, use atomic append/update or optimistic versioning, designate one persistence authority, and make file fallback atomic or explicitly single-process-only. |
| F-042 | Medium | Confirmed | Open | LibreChat provider weight is implemented as repeated non-idempotent POST attempts. | `src/services/libreChatService.ts:262-276` duplicates a provider 1-10 times in the attempt list; model POST loops at `645-750` and `1103-1116` can replay the same logical generation after timeout/5xx without an idempotency key. | Weight increases request replay, latency, provider cost, and duplicate generations instead of merely influencing selection probability. | Use weight only for selection/ordering; define a bounded retry budget and require stable request/idempotency identifiers for any model POST that may be replayed. |
| F-043 | High | Confirmed | Open | Default proxy trust and scattered raw forwarding-header parsing permit client-IP spoofing in directly exposed deployments. | `src/app/assembly.ts:192-218` defaults Express `trust proxy` to one hop; default Compose publishes `3000:3000` directly (`docker-compose.yml:3-13`). At least 28 production files also read `x-forwarded-for`/`x-real-ip` themselves, including `src/controllers/humanCheckController.ts:20-22`, `src/routes/ipVerificationRoutes.ts:14-18`, `src/middleware/apiKeyAuth.ts:58-59`, `src/middleware/ipBanCheck.ts:141-166`, and `src/utils/ipUtils.ts:18-70`. | A direct client can influence IP-based rate limits, bans, replay/nonce decisions, fraud controls, and audit attribution. Different parsers can disagree about the same request. | Configure exact trusted proxy CIDRs/hops per deployment and centralize one client-IP resolver that ignores forwarding headers outside that trust boundary. |
| F-044 | Medium | Confirmed | Open | Three authentication middleware families implement incompatible cookie, suspension, identity, and token semantics. | Canonical `src/middleware/authenticateToken.ts:13-49` supports bearer plus `synapse_token`, loads the user, and checks suspension. `src/middleware/authMiddleware.ts:64-120` and `src/middleware/auth.ts:7-43` are bearer-only with different status/disabled/sub semantics. Nineteen route modules import the bearer-only variants, including data collection, API keys, CDKs, GitHub billing, audit/email/short URLs/resources, FBI/webhooks/markdown, administrator profile/index, TTS/OAuth/LinuxDo/status, LibreChat, and IPFS. | The same valid cookie session succeeds on some APIs and fails on others; disabled/suspended/role behavior depends on route choice, and security fixes must be repeated across variants. | Replace variants with one canonical auth context and explicit optional/required/admin policies; add a route-governance test that enumerates each protected route and its accepted session types. |
| F-045 | High | Confirmed | Open | `/api/auth/me` authenticates the cookie in middleware and then rejects it again in the controller unless a Bearer header exists. | The route mounts canonical `authenticateToken` at `src/routes/authRoutes.ts:184`, which sets `req.user`; `src/controllers/authController.ts:750-773` ignores that context, re-parses only Authorization, and re-verifies the JWT. The frontend intentionally performs a cookie-first request without a bearer when no injected token exists (`frontend/src/hooks/useAuth.ts:210-213`). | A normal HttpOnly-cookie login receives a deterministic 401 during the root authentication probe, breaking session restoration and compounding F-007's route-guard split. | First remove secret fields from the current-user projection under F-046, then have the controller use the middleware-established auth context and add real cookie login/me/logout tests. |
| F-046 | High | Confirmed | Open | TOTP seeds and recovery codes are stored in plaintext and included in ordinary user/admin response projections. | Schema and writes are `src/services/userService.ts:30-32` and `src/controllers/totpController.ts:659-672`; recovery-code verification performs plaintext comparison (`src/services/totpService.ts:235-272`). `PUBLIC_USER_SELECT`, `getUserById`, and the admin projection select `totpSecret`/`backupCodes` (`src/services/userService.ts:84-123,212-226`). `/auth/me` removes password fields only (`src/controllers/authController.ts:789-800`), administrator sanitization also removes only passwords (`src/controllers/adminUserListHelpers.ts:9-17,51-65,90-112`), and an ordinary authenticated user can fetch every remaining recovery code without fresh step-up (`src/controllers/totpController.ts:580-603`). | XSS/session compromise, administrator list access, or database read access can clone the TOTP seed and recovery credentials, defeating the second factor and enabling durable account takeover. | Envelope-encrypt TOTP seeds outside generic projections; display recovery codes only once and store salted hashes; require recent step-up to regenerate; define explicit public/current/admin DTO allowlists before fixing F-045. |
| F-047 | Medium | Confirmed | Open | Authentication and recovery codes are generated with `Math.random()`. | TOTP recovery codes use `Math.random()` in `src/services/totpService.ts:205-225`; email-change codes use it in `src/services/profileUpdateVerificationService.ts:51-56,104-144`; an older email code path uses it at `src/controllers/authController.ts:500-507`. | These values are security credentials, but the generator is not designed for cryptographic unpredictability. | Use `crypto.randomInt`/`randomBytes`, preserve strict expiry/attempt limits, and remove the old path if it is retired under F-058. |
| F-048 | Medium | Suspected | Open | Credentialed CORS trusts every HTTPS sibling subdomain under two parent domains. | `src/middleware/corsMiddleware.ts:75-90` accepts arbitrary subdomains of `hapxs.com` and `chloemlla.com` with credentials enabled. The session cookie is HttpOnly/SameSite=Lax (`src/utils/authCookie.ts:32-39`), and cookie-only requests bypass HMAC replay headers (`src/middleware/replayProtection.ts:74-82`). | Any compromised, delegated, or user-controlled sibling origin could issue credentialed same-site requests and read responses. Actual exposure depends on DNS/hosting ownership outside the repository. | Inventory every sibling origin and replace suffix trust with an exact allowlist or controlled registration policy. Decision question Q-004: are all present and future sibling subdomains fully trusted to act with production user credentials? |
| F-049 | Low | Confirmed | Open | Public CORS responses combine wildcard origin with credentials enabled. | `src/middleware/corsMiddleware.ts:120-134` emits `Access-Control-Allow-Origin: *` with credentials for Turnstile public endpoints mounted at `src/app/assembly.ts:308-311`. | Browsers reject the wildcard-plus-credentials combination, so the advertised cross-origin credential behavior cannot work consistently. | Remove credentials for truly public endpoints or reflect an exact allowed origin with `Vary: Origin`. |
| F-050 | Medium | Confirmed | Open | Mongo proxy URLs can be logged with embedded credentials. | `src/services/mongoService.ts:38-41,120-134` logs the proxy value multiple times; the Mongo URI has masking logic but the proxy URL does not. | A `user:password@host` proxy URL enters console/combined/error logs and any downstream log aggregation or support bundle. | Parse and log only scheme/host or apply one URL-secret redactor to every configuration/log path. |
| F-051 | Low | Confirmed | Open | Caller-controlled `x-request-id` is accepted without length/character validation and persisted as the authoritative correlation ID. | `src/middleware/requestId.ts:12-15` uses any supplied value; audit persistence paths include `src/middleware/auditLog.ts:91-115` and `src/services/auditLogService.ts:618-640`. | Attackers can create oversized/high-cardinality audit records, confusing control characters, or correlation-ID impersonation. | Generate an internal UUID, retain a separately named bounded upstream ID, and enforce a small character/length contract. |
| F-052 | High | Confirmed | Open | Competing signal handlers call `process.exit` before asynchronous HTTP, WebSocket, worker, and persistence drains complete. | The HTTP server is not exported into one shutdown owner (`src/app/startup.ts:185-191`). `src/services/embeddedRustServices.ts:350-371` exits immediately, while `src/services/ip.ts:839-879` and `src/services/dataCollectionService.ts:218-251` attempt asynchronous flushes and also exit. Node does not await async signal listeners. | Deployment or host termination can drop in-flight requests, WebSocket events, scheduled work, and queued/pending writes. Multiple handlers race, so shutdown ordering is nondeterministic. | Implement one shutdown coordinator: stop readiness/new traffic, close HTTP/WS/schedulers/workers, flush queues/stores, close Mongo, then use one bounded forced-exit timer. |
| F-053 | Medium | Confirmed | Open | Three outbound GET paths have no application deadline or cancellation. | Lottery blockchain seed retrieval is `src/services/lotteryService.ts:80-95`; the public network-IP proxy is `src/routes/networkRoutes.ts:975-983`; the hourly LibreChat GitHub scrape is `src/services/libreChatService.ts:333-382`. Other sampled critical providers generally have 5-60 second deadlines. | A half-open connection can stall a request/fallback indefinitely or allow hourly intervals to overlap and accumulate work. | Add an `AbortSignal` total deadline and explicit resource budget; these safe GETs do not need unbounded retries. |
| F-054 | Medium | Confirmed | Open | The generic webhook endpoint accepts and stores unauthenticated event claims. | `src/routes/webhookRoutes.ts:14-20` is public; `src/controllers/webhookController.ts:5-49` explicitly receives provider/source/event data without a signature and persists it. | An attacker can forge provider/source/event records, polluting operational history and any downstream manual or automated decisions. | Require per-source signature verification. If the endpoint is intentionally public intake, label records `untrusted`, isolate them from verified events, and apply strict schema/rate limits. |
| F-055 | Medium | Confirmed | Open | Webhook persistence failures return HTTP 200, preventing provider retry. | Generic webhook error handling at `src/controllers/webhookController.ts:41-49` and signed Resend handling at `95-116` catch storage failures and respond successfully. | A real event can be permanently lost while the sender records delivery success. | Return 5xx on durable-storage failure or acknowledge only after writing to a durable queue/outbox; alert on backlog and dead-letter conditions. |
| F-056 | Low | Confirmed | Open | Webhook retries have no event-level idempotency key or uniqueness constraint. | `src/services/webhookEventService.ts:24-46` defines no provider event-ID unique key, and `212-223` always inserts a new record. | Legitimate provider retries create duplicate history and statistics, complicating reconciliation. | Normalize provider event IDs and use a unique index/upsert; when no ID exists, use a bounded content/time-window digest with documented collision behavior. |
| F-057 | Medium | Confirmed | Open | IPFS settings can reconnect Mongoose to a different database authority. | `src/services/ipfsService.ts:234-238` directly calls `mongoose.connect(process.env.MONGO_URI \|\| "mongodb://localhost:27017/tts")`, ignoring `MONGODB_URI`, `MONGO_DB`, proxy/pool/timeout/retry policy, and runtime-config initialization; settings writes call this path at `1128-1139,1349-1364`. | After a disconnect, one feature can attach the shared Mongoose singleton to localhost or the wrong database, producing split-brain configuration/data behavior. | Make the central Mongo service the only connection owner; feature services should await readiness and never choose a URI. |
| F-058 | Medium | Confirmed | Open | Public legacy email-verification and password-reset endpoints have no reachable state producer. | Current register/forgot-password flows use persistent verification tokens (`src/controllers/authController.ts:334-362,993-1019`). `/verify-email` and `/send-verify-email` only read process `emailCodeMap` (`404-528`) with no initial `set`; old `/reset-password` similarly only reads `resetPasswordCodeMap` (`1133-1212`). Routes remain public at `src/routes/authRoutes.ts:233,254,352`, and `openapi.json:293,322` plus `docs/BACKEND_INTEGRATION_CONTRACT.md:412-432` still advertise them. | Consumers receive deterministic “request first/not found” failures from documented compatibility endpoints, while maintainers may mistake the dead in-memory maps for supported recovery state. | Identify external consumers, then either restore these endpoints to the canonical persistent-token authority with tests or formally deprecate/remove them with versioned documentation. |
| F-059 | Medium | Confirmed | Open | Sensitive-field masking is not attached to the default logger used by most production code. | `src/utils/logger.ts:25-51` creates and exports raw Winston methods; masking exists only in `safeLog` at `54-91`. Only `src/middleware/authMiddleware.ts:102,117,129` uses `safeLog`, while 152 production imports use the raw logger. Raw body/token examples include `src/controllers/ecoEnchantsController.ts:137-742`, `src/controllers/emailController.ts:59-712`, `src/controllers/lotteryController.ts:36`, `src/controllers/policyController.ts:190`, `src/routes/passkeyRoutes.ts:62-316`, and `src/routes/libreChatRoutes.ts:412`; F-037 proves a concrete token leak. | Email content, licenses, credentials, Passkey material, tokens, and user input can enter combined/error/console sinks and support artifacts. | Apply structured redaction inside the default logger methods, prohibit whole-body logging, and add tests/lint rules that representative secrets never reach a sink. |
| F-060 | High | Confirmed | Open | The command-execution endpoint requires only a shared operation password, not a current authenticated administrator. | `POST /api/command/execute` at `src/routes/commandRoutes.ts:181-205` validates body command/password but has no canonical session/role middleware. `src/services/commandService.ts:81-147,467-484` applies a whitelist/character filter, then executes allowed Linux commands with `spawn` and `shell:false` at `321-360` (Windows path `223-290`). Allowed actions include system/network reconnaissance such as process, host, route, DNS, and interface commands. The shared password comes from runtime `ADMIN_SECURITY` through `src/utils/adminOperationPassword.ts:24-37` and is exposed to F-030. | Disclosure of the shared database/configuration password permits remote OS-level reconnaissance and command execution without any revocable user session or current-role check. | Require canonical authentication plus current admin/suspension checks; use the shared factor only for fresh step-up; replace free-form command strings with fixed typed operations and least-privilege execution. |
| F-061 | Medium | Confirmed | Open | Shared-log retrieval relies on a shared password or an administrator's password but no authenticated administrator session. | `POST /sharelog/:id` at `src/routes/logRoutes.ts:356-430` has no canonical auth. `132-165` accepts the global operation password or a matching administrator password, then `378-428` reads Mongo or `data/sharelogs` content. ID/path validation prevents the sampled traversal path but does not establish a user identity. | A leaked password gives public callers access to operational logs and cannot be revoked by terminating a session; logs may contain upstream errors/request details compounded by F-059. | Require a current admin session/role, use password/MFA only as step-up, apply response redaction, and audit a stable administrator identity rather than a shared secret. |

| F-062 | High | Confirmed | Open | The custom dependency-submission workflow fabricates an inaccurate graph and reports success even when submission fails. | `.github/workflows/dependency-graph-submit.yml:33-80` merges runtime and development dependencies, writes declared semver ranges into package URLs as if resolved, marks every entry runtime/direct, and submits no transitive edges. Per-manifest errors are swallowed at `117-119`, the top-level catch only logs at `135`, the step is `continue-on-error` at `141`, and `143-152` unconditionally claims all snapshots succeeded. | Dependency inventory, runtime scope, security review, and exports can be wrong or absent while CI presents a success statement; resolved vulnerable transitives are missing. | Use GitHub's supported pnpm/lockfile submission or a parser that emits exact resolved versions, scopes, and edges; fail on any API/manifest error and build the summary only from verified responses. |
| F-063 | Medium | Confirmed | Open | The intended nightly live suite is non-hermetic and has no implemented missing-secret skip contract. | `.github/workflows/nightly-test-matrix.yml:41-53` always invokes the suite and passes repository secrets without preflight. `src/tests/ipfs-upload.test.ts:23-30` calls a hard-coded public relay; `network-apis.test.ts:5-52`, `media-social-life-apis.test.ts:12-58`, `ip-query.test.ts:5-35`, and `yiyan-api.test.ts:4-39` depend on mutable public providers. `docs/test-matrix.md:49` says missing credentials cause a skip, but no such condition exists. | After F-005 is repaired, third-party outages, DNS/content/rate-limit changes, or absent secrets can create noisy failures and billed traffic, obscuring application regressions. | Keep hermetic provider contracts as required tests; isolate live probes with explicit secret preflight/skip status, strict deadline/retry/spend ceilings, schema assertions, and an escalation owner. |
| F-064 | Medium | Confirmed | Open | Required CI has no coverage baseline or threshold, and the coverage-enabled script is unused. | `jest.config.js:52-71` disables collection by default and defines no threshold. `.github/workflows/tsc.yml:48-52` runs Jest without coverage; `package.json:35` defines `test:ci` with coverage, but no workflow invokes it. | Empty, copied, globally mocked, or uncollected tests can accumulate without a measurable regression signal, especially around high-risk/high-change modules. | Publish the honest current baseline and introduce gradual per-module thresholds for auth, authorization, persistence, deployment helpers, and other critical boundaries rather than one permissive global number. |
| F-065 | Low | Confirmed | Open | Two advertised test scripts point to files that do not exist. | `package.json:33-34` defines `jest performance.test.ts` and `jest concurrency.test.ts`; no tracked `performance.test.*` or `concurrency.test.*` exists. | Contributor and automation entrypoints imply performance/concurrency coverage that cannot run. | Restore real suites under stable paths or remove/rename the scripts; add a lightweight manifest check that public targeted-test paths exist. |
| F-066 | High | Confirmed | Open | Production images are published and deployed without a vulnerability scan, SBOM, signature, or provenance/attestation gate. | `.github/workflows/docker.yml:114-145` logs in and immediately builds/pushes; `151-199` deploys the result. No Trivy, Grype, Syft, Docker Scout, Cosign, SBOM, attestation, or provenance step exists in workflows/manifests/scripts. | A known-vulnerable or substituted artifact can move directly into production, and incident responders cannot prove source/build identity or enumerate the running image's components. | Generate an SBOM for the exact digest, enforce an agreed vulnerability policy, publish signed provenance, sign the digest, and verify attestations before promotion/deployment. |
| F-067 | Medium | Confirmed | Open | CI, standalone frontend builds, and production images use divergent toolchains and mutable base tags. | Rust is pinned to `1.89.0` in `rust-toolchain.toml:2`/CI but Docker uses `rust:1.91-alpine3.22` at `Dockerfile:85,122,159,196,234`. Node appears as 20, 22, 24, and 24.3 (`dependency-graph-submit.yml:31`, `docker.yml:35,86,173`, `Dockerfile:4,58,272`); `frontend/Dockerfile:2` uses Node 18. CI Mongo uses mutable `mongo:7`, and Docker bases are tags rather than digests. | Code can pass under one runtime/compiler and fail or behave differently in the shipped image; upstream tag movement changes artifacts without a repository change. | Define one supported Node/Rust matrix, align CI/Docker, pin external images by digest, and move versions through reviewed automated updates. |
| F-068 | Medium | Confirmed | Open | The runtime image installs development dependencies and suppresses the lifecycle build of a declared native runtime package. | `Dockerfile:296-303` labels the step production dependencies but runs unfiltered `pnpm install --frozen-lockfile --ignore-scripts`. `@cathodique/mmap-io` is a runtime dependency (`package.json:80`) whose install script builds/downloads the binding (`node_modules/@cathodique/mmap-io/package.json:49-59`); `src/services/rustSharedMemoryIpcClient.ts:244-275` falls back to file-backed I/O when native loading fails. | The production image carries avoidable development tooling/attack surface and can silently diverge from the intended mmap performance path while still starting. | Build and verify native artifacts in a controlled builder, install/copy production-only dependencies into runtime, and surface expected-binding fallback as explicit readiness/telemetry. |
| F-069 | Medium | Suspected | Open | The standalone frontend Dockerfile appears unsupported by the current toolchain, but its external-consumer status is unknown. | `frontend/Dockerfile:2-14` uses Node 18 and unlocked `npm install`, then floating `nginx:alpine` at `17-23`. Current Vite requires Node `^20.19.0` or `>=22.12.0` (`frontend/package.json:119`, `frontend/node_modules/vite/package.json:178-180`). No workflow/Compose/static reference to this Dockerfile was found. | If supported, builds can fail or resolve a different graph outside Vite's supported Node range; if obsolete, it is a misleading deployment surface. | Decision question Q-005: is `frontend/Dockerfile` a public/supported build contract? If yes, use the pnpm lockfile and pinned supported images; if no, retire it after external-use confirmation. |
| F-070 | Medium | Confirmed | Open | Deployment guidance and automation identify production artifacts through mutable tags instead of captured digests. | Compose and README use `happyclo/tts-node:latest` (`docker-compose.yml:5`, `readme.md:498-515,919-945`). `.github/workflows/docker.yml:94-145` publishes branch/short-SHA tags, and `186-199` reconstructs the short-SHA tag for deployment rather than passing the build digest. | Repeating a command/tag need not retrieve identical bytes; rollback/audit cannot prove artifact identity, and friendly tags can be overwritten. | Promote and deploy the exact build digest, record it with the release/deploy log, and keep branch/latest tags only as discovery aliases. |
| F-071 | Medium | Confirmed | Open | Containers lack healthchecks, and one endpoint conflates liveness with dependency readiness. | The final image ends at `Dockerfile:268-329` without `HEALTHCHECK`; no Compose service at `docker-compose.yml:3-130` defines one. `/health` returns 503 when Mongo/startup dependencies degrade (`src/routes/healthRoutes.ts:9-39`), while detailed diagnostics are admin-only (`41-49`); there is no separate process liveness route. | Deployment/orchestration cannot distinguish an alive process with an unavailable dependency from a dead process, gate traffic on readiness, or verify a replacement before cutover. | Add minimal `/live` and dependency-aware `/ready`, retain authenticated detail, define Docker/Compose healthchecks, and make deployment wait for readiness. |
| F-072 | Medium | Confirmed | Open | Production observability and incident response are recommendations, not an implemented operational contract. | `docs/backend-optimization-draft.md:440-477,510-518` proposes future metrics and live/ready endpoints. No central Prometheus/OpenTelemetry/Sentry/Datadog/alert path was found; the only runtime health surface is `src/routes/healthRoutes.ts:9-49`. | Error, latency, saturation, fallback, deployment identity, and queue/provider degradation can remain invisible until users report them; there are no repository-defined alert thresholds or owners. | Define a small SLI set, correlation fields, alert thresholds/owners, and incident runbook; expose metrics or an approved telemetry backend including deploy digest and Rust fallback/readiness. |
| F-073 | Medium | Confirmed | Open | There is no actionable backup/restore, RPO/RTO, or recovery-drill runbook for application data. | `docs/backend-mongo-persistence-detail.md:283-296` only advises backing up selected collections and omits commands, schedule, encryption, retention, restore, validation, owner, RPO, and RTO. No `mongodump`/`mongorestore` workflow exists; `docs/rust-node-hybrid-deployment.md:148-162` covers feature-flag rollback, not state recovery. | Corruption, operator error, storage loss, or failed migration has no repository-defined recovery procedure or tested data-loss/recovery-time expectation. | Document each storage mode's encrypted backup/restore procedure, ownership, retention, invariants, RPO/RTO, and scheduled restore drills with retained results. |
| F-074 | Medium | Confirmed | Open | Normative Trellis specifications describe a different repository architecture and technology stack. | `.trellis/spec/backend/index.md:3,7-16` mandates Bun, `apps/server`, `packages/*`, and a 4+2 bounded-context layout; the real backend is Express under `src` (`src/app.ts:4-16`, `package.json:100`). `.trellis/spec/frontend/index.md:7-18` mandates TanStack Query, Zustand, and `packages/shared`; the current Vite/React tree and manifest do not provide those boundaries. | Agents and contributors instructed to read these specs can place code in nonexistent layers, add unintended dependencies, or reject repository-native patterns. | Regenerate specs from the current tree/manifests, separate aspirational architecture from normative current rules, and add ownership/update-date drift checks. |
| F-075 | High | Confirmed | Open | Package metadata declares MIT while the authoritative license and README impose a custom All Rights Reserved license. | `package.json:78` says `MIT`; `LICENSE:1-39` restricts modification, commercial use, sublicensing, and transfer; `readme.md:1449-1454` explicitly says the project is not MIT. | Registries, scanners, downstream users, and image consumers receive a contradictory legal permission signal and may redistribute/modify under rights the owner did not grant. | Have the copyright owner choose the authoritative terms, then synchronize package metadata, LICENSE, README/badges, published metadata, and image labels; document correction of prior releases. |
| F-076 | High | Confirmed | Open | Frontend dependency installation disables TLS certificate validation. | `frontend/.npmrc:1-4` sets `strict-ssl=false`. The production image copies this file before installing dependencies (`Dockerfile:17-24`), CI installs in the frontend directory (`.github/workflows/quality-guardrails.yml:80-82`), and root `build:frontend` changes into that directory (`package.json:19`). | A registry-path/network attacker can present an untrusted certificate during dependency retrieval, undermining lockfile integrity and injecting packages into CI or the production image. | Remove `strict-ssl=false`; configure any private CA through an explicit trusted bundle and make CI fail closed on TLS errors. |
| F-077 | Low | Confirmed | Open | Optional analysis entrypoints can download and execute unpinned remote tooling. | `package.json:59` runs undeclared `npx depcheck`; `frontend/package.json:26` runs undeclared `npx vite-bundle-analyzer`. `.github/workflows/fuck-u-code.yml:35-51` clones mutable tag `v2.2.1` and may run dynamic `go get ./...`; the workflow is read-only/soft-fail, limiting blast radius. Root/frontend `.npmrc` files also leave lifecycle scripts enabled by default outside commands that override them. | Developer/optional-runner code and results can change without a repository change, weakening reproducibility and exposing the runner to mutable third-party code. | Declare lockfile-pinned tools, pin the analyzer commit/release checksum and Go module graph, and maintain an explicit reviewed lifecycle/build-script policy. |
| F-078 | Medium | Confirmed | Open | No executable ownership/maintainer map covers security-, data-, license-, and deployment-sensitive paths. | `git ls-files` finds no `CODEOWNERS`, `OWNERS`, or `MAINTAINERS`. High-change/high-risk paths include `src/controllers/authController.ts`, `src/config/config.ts`, `Dockerfile`, workflows, and `scripts/deploy_image.js`, but no rule requests a domain owner or names an escalation contact. | Authentication, storage/migration, licensing, and production-deploy changes can proceed without automatic specialist review; operational findings lack durable accountable ownership. | Add CODEOWNERS for auth/security, storage/migrations, frontend sessions, CI/release, and licensing/docs; document incident, backup, and dependency-alert escalation owners. |
| F-079 | Low | Confirmed | Open | CI repeats installs and frontend builds without configuring pnpm store caching. | `tsc.yml:30-49` installs root dependencies and runs the root build, which reinstalls/builds frontend (`package.json:13,19`). `quality-guardrails.yml` repeats root installs at `36-37,77-78,108-109,154-155`, plus frontend install/build at `80-86`; its `setup-node` blocks lack `cache`. | Feedback is slower, consumes more runner/network capacity, and increases transient registry-failure exposure. | Configure pnpm cache keyed by both lockfiles, reuse verified artifacts where trust boundaries permit, and retain only genuinely independent jobs. |
| F-080 | Medium | Confirmed | Open | Operator/contributor documentation contradicts current health, image, dependency, and nightly-test behavior. | README claims public `/health` includes `uptime` and `wsConnections` (`readme.md:886-900`), but those are admin-detail fields (`src/routes/healthRoutes.ts:22-49`). It claims a three-stage production-only-dependency image (`readme.md:521-524`), while Docker has 13 stages and installs all dependencies (`Dockerfile:1-303`). `docs/test-matrix.md:49` promises missing-secret skip behavior absent from `.github/workflows/nightly-test-matrix.yml:41-53`. | Operators use incorrect response/deployment expectations, and contributors can mistake non-running/non-hermetic tests for a documented guarantee. | Update authoritative docs after related fixes and add lightweight contract checks for health examples, Docker/runtime claims, and workflow/script references. |
| F-081 | Info | Suspected | Open | Three opaque tracked archives lack repository-visible provenance/checksum manifests, and two have no static consumer reference. | `git ls-files '*.zip'` returns `OpenAI-TTS-Gradio-2025.6.15.zip`, `src/pan.zip`, and `worker.zip`. README identifies `worker.zip` as an optional Cloudflare Worker distribution (`readme.md:256,431,976-979`), but no checksum/source-build record was found; the other two have no static text reference. Archives were not opened in this audit. | Reviewers cannot establish origin, contents, update process, or whether stale/vulnerable binaries are distributed; actual risk depends on external consumers and contents. | Decision question Q-006: who owns/consumes each archive? Retained distributions need source/build provenance, content inventory, checksum, and update policy; otherwise retire only after confirming external contracts. |

### F-001 complete static occurrence list

The interceptor is attached to the shared `api` instance. These are all direct mutating-method calls statically found under `frontend/src` outside tests; dynamic/config-driven calls may add consumers:

- `frontend/src/api/cdks.ts` lines 73, 95, 105, 111, 122, 139, 148, 160
- `frontend/src/api/imageData.ts` lines 23, 33, 47
- `frontend/src/api/librechatAdmin.ts` lines 46, 51, 56
- `frontend/src/api/passkey.ts` lines 55, 70, 84, 93
- `frontend/src/api/resources.ts` lines 54, 60, 66
- `frontend/src/api/rustBenchmark.ts` lines 112, 117
- `frontend/src/api/ticketApi.ts` lines 39, 51, 66, 72, 78
- `frontend/src/api/translationAudit.ts` line 78
- `frontend/src/components/ArtifactSharePage.tsx` line 268
- `frontend/src/components/CommandManager.tsx` lines 144, 214, 258, 363, 429
- `frontend/src/components/EcoEnchantsAdminPage.tsx` lines 369, 397, 426, 499, 524
- `frontend/src/components/EmailSender.tsx` lines 328, 403, 411, 421, 429, 472, 510, 543, 566
- `frontend/src/components/HCaptchaVerificationPage.tsx` line 94
- `frontend/src/components/RegistrationInviteManager.tsx` lines 98, 118, 170
- `frontend/src/components/TOTPManager.tsx` line 138
- `frontend/src/components/TOTPSetup.tsx` lines 53, 83
- `frontend/src/components/TOTPVerification.tsx` line 73
- `frontend/src/components/UserManagement.tsx` lines 462, 492, 571, 626, 1145, 1163, 1310, 1326, 1397, 1429, 1448
- `frontend/src/hooks/useAuth.ts` lines 370, 428, 454

### F-004 complete active occurrence list

- `frontend/src/components/BroadcastManager.tsx` line 101
- `frontend/src/components/BackupCodesModal.tsx` line 52
- `frontend/src/components/CDKStoreManager.tsx` line 346
- `frontend/src/components/FBIWantedManager.tsx` lines 107, 135, 191, 243, 289, 319, 381
- `frontend/src/components/LogShare.tsx` lines 277, 296, 315, 330, 356, 385, 414, 636
- `frontend/src/components/TOTPManager.tsx` line 60

`frontend/src/hooks/useTwoFactorStatus.ts:14` contains the same construction but has no static consumer in the inspected tree; it is tracked separately as a dead-code/ownership candidate rather than counted as an active failure occurrence.

### F-007 complete `useAuth()` occurrence list

- `frontend/src/App.tsx` line 550
- `frontend/src/hooks/usePasskey.ts` line 34
- `frontend/src/hooks/useSidebarView.ts` line 22
- `frontend/src/hooks/useLottery.ts` line 51
- `frontend/src/components/AdminLogin.tsx` line 9
- `frontend/src/components/AnnouncementManager.tsx` line 30
- `frontend/src/components/AuthForm.tsx` line 47
- `frontend/src/components/admin/AdminHub.tsx` line 26
- `frontend/src/components/admin/AdminGuard.tsx` lines 46, 252
- `frontend/src/components/CommandManager.tsx` line 120
- `frontend/src/components/DeepLXTranslatorPage.tsx` line 111
- `frontend/src/components/EmailVerifyPage.tsx` line 10
- `frontend/src/components/EnvManager.tsx` line 249
- `frontend/src/components/ForgotPasswordPage.tsx` line 49
- `frontend/src/components/GoogleAuthButton.tsx` line 117
- `frontend/src/components/LibreChatPage.tsx` line 247
- `frontend/src/components/LinuxDoAuthCallbackPage.tsx` line 28
- `frontend/src/components/LoginPage.tsx` line 106
- `frontend/src/components/LogShare.tsx` line 192
- `frontend/src/components/LotteryAdmin.tsx` line 443
- `frontend/src/components/LotteryPage.tsx` lines 133, 433
- `frontend/src/components/MobileNav.tsx` line 100
- `frontend/src/components/ModListEditor.tsx` line 241
- `frontend/src/components/OutEmail.tsx` line 11
- `frontend/src/components/PenaltyAppealActions.tsx` line 94
- `frontend/src/components/ProviderBindPage.tsx` line 86
- `frontend/src/components/RegisterPage.tsx` line 61
- `frontend/src/components/ResetPasswordLinkPage.tsx` line 47
- `frontend/src/components/ResourceStoreApp.tsx` line 8
- `frontend/src/components/ShortLinkManager.tsx` line 48
- `frontend/src/components/ShortUrlMigrationManager.tsx` line 33
- `frontend/src/components/TicketSystem.tsx` line 77
- `frontend/src/components/UserManagement.tsx` line 201
- `frontend/src/components/WelcomePage.tsx` line 173

## Root-cause clusters

- **Identity has no single authority.** F-004/F-007/F-008/F-040/F-043-F-049/F-060/F-061 show browser state, cookies, bearer tokens, WebSocket claims, IP identity, shared passwords, and three middleware families making different decisions. Repair order: define one request/session/admin context, remove secrets from its DTOs, then migrate every HTTP/WS route and frontend client to it.
- **Secrets are treated as ordinary application data.** F-016/F-029/F-030/F-035/F-037/F-046/F-050/F-059/F-076 share missing secret lifecycle controls: values are recoverable, broadly projected, logged, duplicated, or fetched over an untrusted TLS path. Central redaction alone is insufficient; storage, projection, transport, rotation, and custody must be designed together.
- **Mutable caches and multiple persistence authorities lack concurrency protocols.** F-006/F-032/F-033/F-041/F-055-F-057 cover mocked boundaries, stale configuration snapshots, repeated connection ownership, Mongo/memory/file chat state, webhook acknowledgement, and feature-local database reconnection. Establish one owner per datum and version/atomicity rules before decomposing files.
- **Retry, idempotency, and lifecycle behavior are best-effort.** F-001/F-002/F-014/F-042/F-052/F-055/F-056 combine ambiguous mutation replay, destructive deploy steps, competing shutdown handlers, and event delivery without durable acknowledgement/deduplication. The common fix is explicit state-machine transitions with stable operation IDs and verified terminal conditions.
- **The verification system tests replicas or bypasses real boundaries.** F-005/F-006/F-020/F-023-F-028/F-063-F-065 show ignored suites, global mocks, copied servers/algorithms, weak assertions, missing frontend execution, and no coverage baseline. This explains how concrete auth, storage, and release defects coexist with substantial test volume.
- **Release is an independent mutation path rather than promotion of a verified artifact.** F-002/F-003/F-012-F-014/F-062/F-066-F-071/F-075/F-076 show verification/deploy graph separation, weak host/artifact identity, mutable tags/bases, missing attestations, and absent readiness/rollback evidence.
- **Normative guidance and ownership drift from executable reality.** F-010/F-018/F-019/F-024/F-058/F-074/F-078/F-080/F-081 retain disabled/dead surfaces, mixed-responsibility units, stale scripts/specs/contracts, and unidentified ownership. Cleanup should follow consumer discovery and characterization, not size-only rewrites.
- **Performance and operations are weakly observable.** F-017/F-022/F-033/F-053/F-068/F-071/F-072/F-079 identify broad browser work, fragmented deadlines, redundant initialization, silent fallbacks, absent probes/metrics, and duplicated CI work. Runtime profiling is required before numerical performance claims.

## Treatment options and phased plan

Options are recommendations only under E0/G0/Q0; no remediation is authorized by this report.

| Option | Benefit | Risk reduction | Time to value | Cost | Migration risk | Reversibility |
| --- | --- | --- | --- | --- | --- | --- |
| Containment-first | Stops the most dangerous deploy, auth, secret, and command paths without broad redesign | Very high for immediate security/release exposure | Days to 2 weeks | Medium | Low if feature flags/route denial are used | High |
| Contract consolidation | Creates one auth/request/config/persistence authority and removes repeated drift | High and systemic | 2-8 weeks | High | Medium; touches many consumers | Medium-high with adapters and staged rollout |
| Assurance/release reconstruction | Makes tests, artifact identity, readiness, rollback, backup, and telemetry trustworthy | High for recurrence prevention | 3-10 weeks | High | Low-medium | High when introduced as parallel gates |
| Structural retirement/decomposition | Reduces supermodules, stale specs, dead endpoints/files, and unsupported artifacts | Medium, primarily change safety | Incremental over quarters | Medium-high | Medium where external consumers are unknown | Medium if compatibility shims are time-boxed |

| Slice | Purpose | Change | Verification | Rollout | Rollback | Exit condition |
| --- | --- | --- | --- | --- | --- | --- |
| 0. Freeze unsafe promotion | Prevent an unverified/destructive deployment while fixes start | Disable automatic production mutation or require protected environment approval; fix SSH host verification, error propagation, concurrency, exact digest, TLS validation, and retain rollback image/container | Workflow policy/static tests plus a disposable-host failure drill in GitHub Actions | One production target at a time | Re-enable previous manual procedure only with retained old container/image | Failed pull/start/readiness produces a red job and automatic/verified restore; deployed digest equals tested digest |
| 1. Contain identities and secrets | Remove direct account/host takeover paths | Disable password reveal; protect command/log routes with current admin session; define allowlisted current/admin DTOs; remove MFA/token/secret projections; rotate exposed credentials; fix `/auth/me` only after DTO containment | Real cookie/Bearer/Passkey/TOTP/admin negative tests against production middleware; secret-not-in-response/log assertions | Feature flags and forced credential rotation | Temporarily disable affected feature rather than restore secret exposure | No original password recovery; no auth material in public/admin DTOs/logs; cookie login/me/logout succeeds |
| 2. Consolidate contracts and data authority | Eliminate repeated auth/config/chat/webhook failure modes | One auth context for HTTP/WS, one IP resolver, immutable versioned runtime config, one Mongo owner, unique/CAS chat persistence, durable/idempotent webhook intake, centralized deadlines/retry metadata, one shutdown coordinator | Fault-injection and concurrency tests for stale writes, lost responses, shutdown, event retry, role/suspension changes, and multi-instance chat | Shadow reads/version telemetry before switching writes | Retain old reader temporarily; roll back by configuration generation, never by restoring plaintext secrets | One authority/version per datum; no duplicate/lost update under defined concurrency tests |
| 3. Rebuild verification and release evidence | Make green CI mean the production boundaries were exercised | Remove global boundary mocks from integration configs; run frontend Vitest; real app cookie browser tests; coverage baselines; hermetic provider contracts; supported dependency graph; SBOM/scan/sign/provenance | Required jobs for exact commit/digest; known test counts; fault injection; attestation verification | Make new jobs informational, then required after stable baseline | Temporarily relax a threshold with owner/expiry, not skip the job silently | Required checks gate the digest; no empty/copied/no-op suite is presented as integration evidence |
| 4. Operational recovery | Detect and recover failures before user reports | Separate live/ready, Docker healthchecks, metrics/alerts/runbooks, encrypted backups, RPO/RTO, restore and rollback drills | Scheduled restore/deploy-failure drills with retained artifacts and owner acknowledgement | Stage on non-production then one production host | Documented previous digest and restore point | Readiness gates cutover; alert and restore drills meet declared objectives |
| 5. Decompose and retire | Reduce future change cost without rewrite risk | Split demonstrated supermodules by use case; resolve ModList and legacy email APIs; regenerate Trellis/docs; remove only consumer-free files/Dockerfile/archives | Characterization tests, import/consumer searches, compatibility telemetry, documentation checks | Small reversible slices | Restore shim/entry only within a declared compatibility window | Each slice removes a proven change reason/authority duplication and has an owner |

## Compatibility and data migration

- Compatibility execution is **N/A in this E0 audit**: C1 is the planning default, but no API, response, configuration, storage, binary, or user-visible contract was changed. F-010/F-058/F-069/F-081 require explicit consumer discovery before any retirement.
- Data migration execution is **N/A under D0**. Future remediation of F-029/F-030/F-035/F-041/F-046 would require expand-first schemas, versioned encryption/hash fields, backfill with reconciliation, shadow reads, explicit cutover, credential rotation, and a rollback that never reintroduces plaintext/recoverable secrets.

## Pending user decisions

| Question | Finding | Decision needed | Why repository evidence is insufficient | Answer/status |
| --- | --- | --- | --- | --- |
| Q-001 | F-022 | Is both always-on whole-DOM anti-tamper scanning a required production contract, and is callback/long-task/false-positive telemetry available? | Product anti-tamper requirements and production device/page profiles are external to the repository. | Open — user response requested |
| Q-002 | F-026 | Does a GitHub fault-injection job prove unhandled rejections/exceptions fail despite the installed process listeners and mocked console? | Listener ordering/timing must be demonstrated in the prohibited CI/runtime execution environment. | Open — user response requested |
| Q-003 | F-010 | Should ModList be restored as a supported feature or formally retired? | Navigation/docs say supported, while the active page says temporarily closed; product roadmap/consumers are not derivable from code. | Open — user response requested |
| Q-004 | F-048 | Are all current and future sibling subdomains under `hapxs.com` and `chloemlla.com` fully trusted with production credentials? | DNS ownership, delegated hosting, and subdomain inventory are outside the repository. | Open — user response requested |
| Q-005 | F-069 | Is `frontend/Dockerfile` a supported/public build entrypoint? | No internal consumer exists, but external CI/users may invoke it. | Open — user response requested |
| Q-006 | F-081 | Who owns and consumes each tracked ZIP archive, and may the unreferenced archives be retired? | Archives were intentionally not opened; external distribution/consumer state is unavailable. | Open — user response requested |

## Changes and verification

| Finding | Before evidence | Change | After evidence / verification | Result |
| --- | --- | --- | --- | --- |
| Audit report | No repository-wide legacy audit report existed | Created and consolidated this write-through report only | Ledger parser confirms continuous F-001-F-081, no duplicates/missing IDs, 30 High / 43 Medium / 7 Low / 1 Info, and 76 Confirmed / 5 Suspected; Markdown table separators statically checked | Complete for E0 |
| Product/runtime code | Existing dirty user worktree preserved | No code, configuration, schema, workflow, data, or documentation other than this report changed | `git status --short` reviewed before delivery; only this report is staged/committed by the audit | Unchanged |
| Build/test/type-check/lint/install | Repository prohibits local execution | None run locally | Workflow definitions and test implementations inspected statically; lack of execution remains an explicit limitation | Not executed by policy |
| Compatibility/data | C1/D0 | No contract or data migration performed | User-visible/runtime behavior remains unchanged | N/A |

## Documentation feedback

- Functional standards consulted: README/operator guidance, route annotations/OpenAPI, backend integration and persistence docs, schemas/storage services, test matrix/configuration, Docker/Compose/workflows/scripts, package metadata/lockfiles, LICENSE, and Trellis project specs.
- Documentation updated: This audit report only.
- Missing/stale documentation findings: health response and image-stage/runtime-dependency claims (F-080), live-test skip behavior (F-063/F-080), dead legacy auth endpoints in OpenAPI/integration docs (F-058), wrong Trellis architecture (F-074), contradictory license metadata (F-075), and absent ownership/incident/backup/restore/RPO/RTO/artifact-provenance contracts (F-066/F-072/F-073/F-078/F-081).

## Remaining risk and uninspected areas

- Deferred/accepted/open findings: all 81 findings remain Open because E0/G0/Q0 authorizes reporting only; none is marked Fixed, Deferred, Accepted, or Rejected. Five remain Suspected pending Q-001/Q-002/Q-004/Q-005/Q-006; Q-003 is a product remediation decision for a Confirmed issue.
- Checks not run and why: All local build, test, type-check, lint, and installation commands are prohibited by repository instruction; GitHub workflow evidence will be used where possible.
- External state not inspected: GitHub Actions run history, branch protection/rulesets, Environments/approvals, repository secrets, Dependabot/security UI, Docker Hub tag/digest/attestation state, production hosts/proxies/databases/object stores/logs/metrics/traffic, actual backups/restores, SLOs, flaky rate, and full sibling-domain inventory.
- Static limits: no complete transitive vulnerability/license-database analysis; no production workload profiling; no fault/concurrency/recovery drills; Rust worker internals were sampled at integration/startup boundaries rather than exhaustively audited; `.env` secrets were not read; the three ZIP archives were not opened.
- Worktree scope: LOC uses Git-tracked worktree files and excludes untracked user files, `.trellis/.agents` from product LOC, generated/dependency/build output, and the audit toolkit. Existing dirty files remain user-owned; the NexAI signing additions extend F-030 but do not create the pre-existing plaintext/config-cache root causes.
- Positive controls to preserve while remediating: production startup validation already requires several core secrets; a canonical cookie-aware auth middleware exists; many sensitive routes have limiters/replay/audit controls; containers run as a non-root user; GitHub actions are generally commit-pinned and CodeQL/Rust locked checks exist; health detail is admin-restricted; API-key billing uses transaction/unique-operation/reconciliation controls. These do not cancel the findings but provide useful migration anchors.
